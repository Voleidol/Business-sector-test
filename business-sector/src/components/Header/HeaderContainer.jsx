import React from "react"
import style from './Header.module.css';


const HeaderContainer = (props) => {
    return(
        <header>
            <h2 className={style.black}>Header</h2>
        </header>
    )
}

export default HeaderContainer